<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MahmoudIbrahimCV" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
    </style>
    
    <!-- Splash Screen Theme - Optimized for fast display -->
    <style name="Theme.MahmoudIbrahimCV.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/dark_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <!-- Removed icon background color for faster rendering -->
        <item name="postSplashScreenTheme">@style/Theme.MahmoudIbrahimCV</item>
        <!-- Keep splash visible only until app is ready -->
        <item name="windowSplashScreenAnimationDuration">0</item>
    </style>
</resources>
